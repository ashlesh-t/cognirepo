graph LR
    subgraph Primary["Primary Agent (DEEP tier)"]
        PA[router.py<br/>DEEP query]
        PA -->|delegates via HTTP| HTTP_C[HTTP adapter<br/>orchestrator/model_adapters/]
    end

    subgraph Secondary["Secondary Agent (FAST tier)"]
        HTTP_S[Model API<br/>Haiku / Flash]
        SA[router.py<br/>FAST lookup]
    end

    HTTP_C -->|REST| HTTP_S
    HTTP_S --> SA
    SA -->|result| HTTP_C
    HTTP_C -->|sub-results| PA
    PA -->|full response| User([User])

    note1[/"Multi-agent mode is OFF by default.<br/>Enable: multi_agent: true in config.json"\]
