# COGNIREPO-106 — Docs truth pass

Epic: COGNIREPO-100 · Branch: story/COGNIREPO-106 · Base: development

## Backstory
Audit-confirmed doc drift (../../COGNIREPO-100-Discovery.md §1c, §2, §5, §6):
- docs/FEATURES.md §15 lists 17 test files; tests/ has 85.
- FEATURES §16/§17 claim CALLS_API/SHARES_SCHEMA are "never auto-detected" — stale:
  intelligence/indexer/http_call_scanner.py + `cognirepo org rewire` + doctor's org CALLS_API
  check (main.py:864) exist. glama.json's link_repos description repeats the stale claim
  (regenerated by 101; verify).
- README.md:609-637 Future Plans headers still say "(v0.3.0)"/"(v0.4.0)" at v2.0.0, and items
  already partially done (Go calls, BM25) aren't annotated.
- SECURITY.md:118 lists Snyk; CI actually runs Bandit/pip-audit/Trivy/TruffleHog
  (.github/workflows/security.yml).
- IMPROVEMENTS.md item 2 is half-stale (search_token/get_agent_bootstrap ARE in the manifest).
- interface/cli/docs_index.py shim's own docstring says "Removed in v2.0" — still shipped.
- docs/MCP_TOOLS.md: spot-fix any defaults drifted vs code.

## Description
One PR fixing all of the above. Delete the shim (in-repo importers already use
intelligence.indexer.docs_index; add a CHANGELOG "Removed" entry). Extend
tests/test_documentation.py to pin FEATURES §15's count to the tests/ glob so it can't rot
again.

**Correction (found during Analyze, 2026-07-21):** `tests/test_documentation.py` no longer
exists — it was deliberately deleted in commit `f17d467` ("prune 76 theatre tests... zero
behavioral signal") along with `test_tool_first_workflow.py`, both of which FEATURES.md §15
still lists as if current (more drift than the ticket's own audit caught). Recreating that
file to satisfy this AC would reverse a reasoned prior decision. The count-pin assertion is
added to `tests/test_docs_sync.py` instead (the still-live, behavioral doc-sync test file),
and §15's table is corrected to drop the two dead filenames and stop claiming to be
exhaustive. Also found live: `docs/MCP_TOOLS.md`'s `org_wide_search` return type
(`list`→`dict`) and `retrieve_memory`'s signature missing `include_org`/`repo_path` — folded
into the "spot-fix defaults" bullet.

## Acceptance criteria
1. Each bullet above corrected; grep proofs in the PR (e.g. no "v0.3.0" under Future Plans, no
   Snyk in SECURITY.md gate table, shim file absent).
2. test_documentation.py enforces the §15 count; suite green.
3. CHANGELOG Unreleased notes the shim removal as a (technically) breaking removal already
   announced for v2.0.

## Risks / notes
- Do after 101/D01 so the tool counts written are generated truth.
- Keep edits factual-only; no marketing rewrites (that's EPIC-600).
